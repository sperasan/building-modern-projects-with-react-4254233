/* eslint-disable no-unused-vars */
import { makeAutoObserverable } from "mobx";

class TodoStore {
  todo = [];

  createTodo = (text) => {};
  markTodoAsCompleted = (text) => {};
  deleteTodo = (text) => {};
}
